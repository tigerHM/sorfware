MySQL集群
特点：
True Multi-master, Active-Active Cluster在任何时间对任何节点进行读写。
同步复制无从延迟，节点崩溃时不会丢失数据。
所有节点保持相同的状态。节点间不允许有发散数据。
为了更好的性能。适用于任何工作负载。
禁止主从切换操作和VIP使用。
热备份在故障转移期间没有停机时间(因为没有故障转移)。
自动发放节点无需手动备份和复制数据库到新节点。
支持InnoDB。
对应用程序透明不需要(或只需要很少的更改)应用程序。
不需要读写分裂。
易于使用和部署

官方文档：https://galeracluster.com/products/
